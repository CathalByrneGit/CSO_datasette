[project]
name = "cso-datasette"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "datasette>=1.0a32",
    "dogsheep-beta",
    "datasette-llm",
    "datasette-agent",
    "llm-gemini",
    "llm-openrouter",
    "httpx>=0.28",
]

[tool.uv]
prerelease = "allow"
